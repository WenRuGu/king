
账号yusong  密码ys81957708  个推
1、没有办法检测用户禁止 某些权限，plus.navigator.checkPermission上说安卓不支持
     返回“unsupported”，但是我都是返回authorized。
	ios可以。但是ios也有其他方法可以获取。
	表示不懂安卓和ios，native也是第一次接触
2、ios不支持transparent背景透明，支持了
3、使用plus.maps,会覆盖div，（使用子窗口）但是会遇到如上问题，ios不支持
	地图上要显示的某些文字，可以使用自定义控件来实现。
4、底部圆形突出按钮，底部单独分离的 webview
5、没有比较好的办法，获取内存，并且清理内存
6、软件盘弹出自带阴影，遮盖样式，虽然影响不大。
7、退出应用不能接收推送消息。
8、选取相册中的二维码扫描，永远都是无法识别，，， 底部做个webview 
	扫码显示会盖住，没有权限就显示出来。
9、可拖动选项卡 webview 模式，两只手指多次点击 a，和下面对不到的bug
	随机点击 a，列表，有几率找显示不出。
10、加载头像，如果是从本地加载（网络下载头像也有可能），极大的消耗内存，
	导致app启动加载webview，dom渲染很慢。
     图片做过缓存的，懒加载。如果是数据变动不频繁的。需要注意加载本地图片的时候
      会不会卡顿，点击不了，事件触发不了
11、扫码全屏，表示小白不知道怎么弄
12、在webview渲染的时候，快速多次点击，渲染感觉卡卡的。
    如果一个页面有多个子 webview，快速多次点击，会找不到页面，无法显示。
13、需要拍照，位置。怎么一开始使用就不提示权限，暗地里直接是允许的（默认）。
14、相机，没有权限会报错和返回是一样的，但是触发时间不一样。以 500 毫秒分割。
15、ios 压缩的图片 src：'_doc/head.jpg'，绝对路径写成相对路径。
16、confirm或者alert会阻止，正在触发的事件
